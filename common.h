#pragma once

#include <numeric>
#include <vector>
#include <iostream>
#include <cmath>
#include <cc_v5.h>
#include <cv.h>
#include <highgui.h>

#pragma comment(lib, "libcaffe5.0.lib")

#define min(a, b)  ((a)<(b)?(a):(b))
#define max(a, b)  ((a)>(b)?(a):(b))

using namespace std;
using namespace cc;
using namespace cv;